http:
  routers:
    task-service-router:
      rule: "Header(`X-Service`, `task-service`)"
      service: task-service
      entryPoints:
        - web

    dashboard-router:
      rule: "PathPrefix(`/dashboard`)"
      service: api@internal
      entryPoints:
        - traefik

  services:
    task-service:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:6061"
  
  